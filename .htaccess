RewriteEngine On

# Если файл/папка существует — отдаём напрямую
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Все остальные запросы -> index.php
RewriteRule ^ index.php [L]
